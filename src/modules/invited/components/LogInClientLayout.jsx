import React from 'react'

const LogInClientLayout = (props) => {
    return (
        <div className='LoginLayout' >
            {props.children}
        </div>
    )
}

export default LogInClientLayout
